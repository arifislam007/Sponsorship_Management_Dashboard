POSTGRES_DB=sombhabona
POSTGRES_USER=sombhabona_user
POSTGRES_PASSWORD=change_this_password
VITE_API_URL=/api/v1
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost

# Lead Management > Google Sheets sync. Paste the full service-account JSON key
# as one line (or base64-encode it first, either is accepted). See README/docs
# for how to create this in Google Cloud Console.
GOOGLE_SERVICE_ACCOUNT_JSON=
